\pagestyle{empty}

\definecolor{coverColor}{HTML}{4087FC}
\newcommand\CoverTitle{ALGORITMI}
\newcommand\CoverSubTitle{Appunti delle lezioni}
\newcommand\Year{2021}

\begin{tikzpicture}[
    remember picture, overlay,
    hexagons/.style={draw, regular polygon, regular polygon sides=6, rounded corners, minimum size=\i cm, ultra thick}
]
% Background
\fill[coverColor] (current page.south west) rectangle (current page.north east);

% Background Polygons
\foreach \i in {0.5,...,22} {
    \node[coverColor!60, hexagons] at ($(current page.north west)+(2.5,0)$) {};
}

\foreach \i in {0.5,...,22} {
    \node[coverColor!90, hexagons] at ($(current page.north east)+(0,-9.5)$) {};
}

\foreach \i in {21,...,6} {
    \node[coverColor!85, hexagons] at ($(current page.south east)+(-0.2,-0.45)$) {};
}

% Title
\node[coverColor!5, left, minimum height=3cm, minimum width=0.625*\paperwidth] at ($(current page.north east)+(0,-9.5)$) {
    {\fontsize{25}{30}\selectfont \bfseries \CoverTitle}
};

% Subtitle
\node[coverColor!10, left, minimum height=2cm, minimum width=0.625*\paperwidth] at ($(current page.north east)+(0,-11)$) {
    {\huge \textit{\CoverSubTitle}}
};

% Version
\node[coverColor!10, left, minimum height=2cm, minimum width=0.625*\paperwidth] at ($(current page.north east)+(0,-12)$) {
    {\large\ttfamily {\docversion} - {\today}}
};

% Author Name
\node[coverColor!5, left, minimum height=2cm, minimum width=0.625*\paperwidth] at ($(current page.north east)+(0,-13)$) {
    {\Large \textsc{\Author}}
};

% Year
\foreach \i in {2.5,...,22} {
    \node[coverColor!60, hexagons] at ($(current page.west)+(2.5,-5)$) {};
}

\node[fill=coverColor!70, text=coverColor!5, regular polygon, regular polygon sides=6, minimum size=2.5 cm, inner sep=0, ultra thick, rounded corners] at ($(current page.west)+(2.5,-5)$) {
    {\LARGE \bfseries \Year}
};

\end{tikzpicture}