% NOTE permette di creare sumature all'interno di un'immagine
\usetikzlibrary{fadings}

% NOTE scelta del font per la copertina
\usepackage{anyfontsize}
\newcommand*{\mytitlefont}{\fontfamily{LibreBskvl-LF}\selectfont}