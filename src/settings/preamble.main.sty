% Precompiled header with XeLaTeX not working
% SEE https://tex.stackexchange.com/questions/359867/
% \RequirePackage[T1]{fontenc}
\documentclass{book}
% NOTE evita di spaziare equamente il contenuto su tutta la pagina
\raggedbottom

\usepackage{../libraries/set-color}
% NOTE personalizzazione delle didascalie
\usepackage{../libraries/set-graphics}

% NOTE impostazione dei percorsi globali
\usepackage{./paths}

\usepackage[compact,explicit,sc]{titlesec}
\usepackage{./redefinitions-chapter}
\usepackage{./redefinitions-part}

% NOTE tabella dei contenuti
\usepackage[none]{tocbibind}
% SEE https://tex.stackexchange.com/a/346097/127473
% \let\oldaddcontentsline\addcontentsline
% \newcommand{\hidefromtoc}{\renewcommand{\addcontentsline}[3]{}}
% \newcommand{\writetotoc}{\let\addcontentsline\oldaddcontentsline}
\usepackage{./pretty-toc}

% NOTE gestione spaziatura documento
\usepackage[
	skip=0pt,     % default: .5\baselineskip plus 2pt
    tocskip=0pt,  % default: \parskip
	indent=0pt,
    parfill=30pt  % default: 30pt
]{parskip}

% https://stackoverflow.com/questions/2318598/
\setlength\partopsep{-\topsep}
\addtolength\partopsep{-\parskip}

% NOTE appendice
\usepackage{appendix}
\renewcommand{\appendixname}{Appendice}
\renewcommand{\appendixpagename}{Appendici}
\renewcommand{\appendixpage}{%
	\thispagestyle{epigraph}%
	{\null\vfill%
	\filleft\fontsize{40}{40}\selectfont\scshape\appendixpagename%
	\vfill\null}%
	\clearpage}

% ATTENTION package "standalone" da caricare presto
\usepackage[subpreambles=false]{standalone}
% \usepackage{subfiles}
% NOTE soluzione per utilizzare il preambolo precompilato del main
\newif\ifsubfile
\subfiletrue

\usepackage{../libraries/set-geometry}

% NOTE in alcune parti del documento è ancora utilizzato
% TODO rimuovere dipendenze e rimuovere pacchetto
\usepackage{multicol}

% NOTE 'xargs' - permette di usare più di un parametro opzionale in un nuovo comando
% NOTE 'xparse' - it offers a replacement for LATEX2ε’s \newcommand macro
\usepackage{comment, xargs, xparse}

\usepackage{../libraries/set-fonts}
\usepackage[useregional, datesep=/, style=ddmmyyyy]{datetime2}

\usepackage{../libraries/set-lists}

\usepackage{tikz}
\usepackage{./cover}
\usepackage{pgfplots}
% SEE https://tex.stackexchange.com/questions/81899
\pgfplotsset{compat=1.16}

% NOTE dichiarazione di 'cell'
\usepackage{../assets/figures/tikz-preamble}

% NOTE dichiarazione nuovi colori
\colorlet{darker}{blue!50!black}
\colorlet{faded}{gray}
\colorlet{mathcolor}{blue}

% NOTE personalizzazione degli alberi
\usepackage{../libraries/set-forest}

% NOTE personalizzazione delle tabelle
\usepackage{../libraries/set-tables}

% NOTE permette di personalizzare le virgolette
% NOTE dovrebbe essere caricato dopo minted, ma minted crea problemi con la cache
\usepackage[autostyle, italian=quotes]{csquotes}

% NOTE utilizzati nei capitoli 13/14
\usepackage{chronology}
\usepackage{pgfgantt}

% NOTE notazione matematica
\usepackage{../libraries/set-math}

% SEE https://tex.stackexchange.com/a/585169/127473
\usepackage{newfloat}
% NOTE personalizzazione dell'aspetto degli algoritmi
\usepackage{../libraries/set-algorithm2e}

% NOTE nomi delle funzioni
\usepackage{../assets/algorithms/functions}

% NOTE utilizzato nel capitolo '16-backtrack'
\usepackage{chessboard}

% NOTE comandi personalizzati
\usepackage{../libraries/set-macros}

% % NOTE impostazione dei metadati del PDF
\usepackage{./pdf-props}
\usepackage{../libraries/set-hyperref}

% NOTE funzione di utilità per accertarmi di aver caricato il preambolo correttamente
\def\preambleloaded{Precompiled preamble loaded.}
