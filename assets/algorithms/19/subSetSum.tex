%&../preamble

% \documentclass[varwidth=6in]{standalone}
% \usepackage{../preamble}

% arara: pdflatex: { synctex: no }
% arara: latexmk: { clean: partial }
\begin{document}
\ifstandalone
\NoCaptionOfAlgo
\begin{algorithm}[H]
\caption[Problema della somma di un sottoinsiee]{}
\fi

\prototype{\Bool \subSetSum{\Array{\Int} \(A\), \Int \(n\), \Int \(k\)}}{

	\BlankLine
	\Array{\Bool} \(DP\) \Assign \new \Array{\Bool}{0}{k}\;

	\BlankLine
	\tcp{casi base}

	\tcp{dati nessun oggetto posso ottenere il valore 0}
	\(\MatrixCall{DP}{0}{0} \Assign \True\)\;

	\For{\(r \Assign 1\) \DownTo \(k\)}{
		\tcp{dati 0 oggetti posso ottenere un valore di \(k > 0\)}
		\(\MatrixCall{DP}{0}{r} \Assign \False\)\;
	}

	\BlankLine
	\tcp{riempi la tabella}
	\From{\(i \Assign 1\) \DownTo \(n\)}{
		\From(\Comment*[h]{\(A[i]\) perché}){\(r \Assign A[i]\) \DownTo \(k\)}{
		\tcp{\(A[i]\): semplifico}

			\BlankLine
			\tcp{logica dell'algoritmo}
			\(\MatrixCall{DP}{i}{r} \Assign \MatrixCall{DP}{i-1}{k}\) \Or \(\MatrixCall{DP}{i-1}{r - A[i]}\)\;
		}
	}

	\BlankLine
	\Return \MatrixCall{DP}{n}{k}\;
}

\ifstandalone
\end{algorithm}
\RestoreCaptionOfAlgo
\fi

\end{document}
