% NOTE per quando precompili il preambolo
\documentclass[varwidth=6in]{standalone}

% OPTIMIZE lo usano
% 06: minDistance
\usepackage{multicol}

\usepackage[usenames,dvipsnames]{xcolor}

% NOTE utilizzato nel comando 'alert', presenti in tutti gli schemi di algoritmi
\colorlet{rose}{red!50!white}

% NB è una copia, copia le modifiche su 'assets/figures/tikz-preamble'
% NOTE questi colori vengono utlizzati nel capitolo '09-grafi'
\colorlet{nodeRed}{red!30}
\colorlet{nodeBlue}{blue!30}
\colorlet{nodeYellow}{yellow!30}

\colorlet{arcoAlbero}{red}
\colorlet{arcoIndietro}{teal} % brown -> teal
\colorlet{arcoAvanti}{purple}
\colorlet{arcoAttraversamento}{blue}

\usepackage{set-math}
\usepackage{set-quote}
\usepackage{../../settings/preamble.forest}
\usepackage{../../settings/macros}

\usepackage[italian]{babel}
\usepackage{csquotes}
\usepackage{xparse} % serve a 'set-algorithm2e'
\usepackage{set-algorithm2e}
\NoCaptionOfAlgo% elimina la scritta 'Algoritmo no.'
\algorithmstyle{tworuled}% l'opzione globale è 'ruled'
\newif\ifFigureOfAlgo% dichiarazione nuovo if
\FigureOfAlgotrue% stampa l'immagine

\usepackage{functions}

% NOTE funzione di utilità per accertarmi di aver caricato il prambolo
\def\preambleloaded{Precompiled preamble loaded.}
